#version 300 es
precision mediump float;
in vec3 a_position;
in vec2 tex_coord;
uniform mat4 world;
uniform mat4 view;
uniform mat4 projection;
out vec2 f_tex_coord;
void main(){
f_tex_coord = tex_coord;
gl_Position = world * view * projection * vec4(a_position, 1.0);
}
